{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "sh -c 'FASTAPI_ENV=production uvicorn app.api_server:app --host 0.0.0.0 --port $PORT --workers 1 --log-level info'"
  }
}